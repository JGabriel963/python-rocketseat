repos:
  - repo: local
    hooks:
      - id: pylint
        name: pylint
        entry: pylint
        language: system
        types: [python]
        args:
          [
            "-rn", # Only display messages
            "-sn", # Don't display the score
            "--rcfile=mvc/.pylintrc", # Link to your config file (relative to git root)
            "--load-plugins=pylint.extensions.docparams", # Load an extension
          ]